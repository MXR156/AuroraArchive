@props(['medium'])<article class="group min-w-0"><a href="{{ route('media.show',$medium) }}" class="relative block aspect-video overflow-hidden rounded-xl bg-zinc-900">@if($medium->thumbnail_url)<img src="{{ $medium->thumbnail_url }}" alt="" class="size-full object-cover transition group-hover:scale-[1.03]" loading="lazy">@else<div class="grid size-full place-items-center text-zinc-700">No thumbnail</div>@endif @if($medium->duration_seconds)<span class="absolute right-2 bottom-2 rounded bg-black/85 px-1.5 py-0.5 text-xs">{{ sprintf('%d:%02d',intdiv($medium->duration_seconds,60),$medium->duration_seconds%60) }}</span>@endif<span class="absolute top-2 left-2 rounded bg-black/80 px-2 py-1 text-[10px] font-semibold uppercase">{{ $medium->status->value }}</span></a><div class="pt-3"><a href="{{ route('media.show',$medium) }}" class="line-clamp-2 text-sm font-semibold leading-5">{{ $medium->title }}</a><p class="mt-1 truncate text-xs text-zinc-500">{{ $medium->channel_name ?: 'Unknown channel' }} @if($medium->published_at) · {{ $medium->published_at->diffForHumans() }} @endif</p></div></article>
