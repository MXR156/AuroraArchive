<x-layouts.app title="Settings"><div class="mx-auto max-w-3xl"><div class="mb-7"><p class="text-sm text-zinc-500">Settings</p><h1 class="text-3xl font-bold">YouTube authentication</h1><p class="mt-2 text-sm text-zinc-400">Upload Netscape-format cookies. The contents are encrypted in MySQL.</p></div><section class="rounded-2xl border border-white/10 bg-zinc-900 p-6"><div class="mb-6 flex items-center justify-between gap-4"><div><h2 class="font-semibold">Status</h2><p class="mt-1 text-sm text-zinc-500">{{ $credential?->status_message ?: 'No database-managed cookies configured.' }}</p></div><span class="badge">{{ str_replace('_',' ',$credential?->status->value ?? 'not configured') }}</span></div><form method="POST" action="{{ route('settings.cookies.store') }}" enctype="multipart/form-data" class="grid gap-4 border-t border-white/8 pt-6">@csrf @method('PUT')<input type="file" name="cookies" required accept=".txt,text/plain" class="field"><button class="primary w-fit">Encrypt and save</button></form>@if($credential)<div class="mt-6 flex gap-2 border-t border-white/8 pt-6"><form method="POST" action="{{ route('settings.cookies.test') }}">@csrf<button class="secondary">Test YouTube authentication</button></form><form method="POST" action="{{ route('settings.cookies.destroy') }}">@csrf @method('DELETE')<button class="danger">Remove cookies</button></form></div>@endif</section><p class="mt-5 text-sm text-zinc-500">Advanced fallback: mount <code>/config/cookies.txt</code>. Database cookies take precedence.</p></div></x-layouts.app>
