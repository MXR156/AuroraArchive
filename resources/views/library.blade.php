<x-layouts.app title="Library"><div class="mb-7 flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-zinc-500">{{ $media->total() }} items</p><h1 class="text-3xl font-bold">Library</h1></div><form class="flex gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search" class="field"><select name="status" class="field"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select><button class="secondary">Filter</button></form></div><div class="video-grid">@foreach($media as $medium)<x-video-card :medium="$medium" />@endforeach</div><div class="mt-10">{{ $media->links() }}</div></x-layouts.app>
