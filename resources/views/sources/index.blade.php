<x-layouts.app title="Subscriptions"><div class="mb-7"><p class="text-sm text-zinc-500">Channels, playlists, and videos</p><h1 class="text-3xl font-bold">Subscriptions</h1></div><div class="grid gap-6 xl:grid-cols-[22rem_1fr]"><form method="POST" action="{{ route('sources.store') }}" class="grid h-fit gap-4 rounded-2xl border border-white/10 bg-zinc-900 p-5">@csrf<h2 class="font-semibold">Add source</h2><select name="type" class="field"><option value="channel">Channel</option><option value="playlist">Playlist</option><option value="video">Individual video</option></select><input name="name" required class="field" placeholder="Display name"><input name="external_id" required class="field" placeholder="YouTube ID"><input name="url" type="url" required class="field" placeholder="https://youtube.com/…"><label class="text-sm text-zinc-400">Check interval (minutes)<input name="scan_interval_minutes" type="number" value="360" min="15" class="field mt-1"></label><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="auto_download" value="1" checked>Automatically download new videos</label><button class="primary">Add and scan</button></form><div class="grid content-start gap-3">@forelse($sources as $source)<article class="flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-white/8 bg-zinc-900 p-5"><div><div class="flex gap-2"><h2 class="font-semibold">{{ $source->name }}</h2><span class="badge">{{ $source->enabled?'Enabled':'Paused' }}</span></div><p class="mt-1 text-sm text-zinc-500">{{ ucfirst($source->type) }} · {{ $source->media_count }} items · {{ $source->last_scanned_at?->diffForHumans() ?? 'Never scanned' }}</p>@if($source->last_scan_error)<p class="mt-2 text-xs text-red-300">{{ $source->last_scan_error }}</p>@endif</div><div class="flex gap-2"><form method="POST" action="{{ route('sources.scan',$source) }}">@csrf<button class="secondary">Scan</button></form><form method="POST" action="{{ route('sources.toggle',$source) }}">@csrf @method('PATCH')<button class="secondary">{{ $source->enabled?'Pause':'Enable' }}</button></form><form method="POST" action="{{ route('sources.destroy',$source) }}">@csrf @method('DELETE')<button class="danger">Remove</button></form></div></article>@empty<div class="rounded-2xl border border-dashed border-white/10 p-12 text-center text-zinc-500">No sources yet.</div>@endforelse</div></div></x-layouts.app>
