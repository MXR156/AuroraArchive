<x-layouts.app title="Home"><div class="mb-8"><p class="text-sm font-medium text-violet-400">Your archive</p><h1 class="text-3xl font-bold tracking-tight">Welcome back, {{ auth()->user()->name }}</h1></div>@foreach([['Continue watching',$continue],['Recently downloaded',$recent],['Latest discovered',$discovered]] as [$heading,$items])@if($items->isNotEmpty())<section class="mb-10"><h2 class="mb-5 text-xl font-semibold">{{ $heading }}</h2><div class="video-grid">@foreach($items as $medium)<x-video-card :medium="$medium" />@endforeach</div></section>@endif @endforeach @if($discovered->isEmpty())<div class="rounded-2xl border border-dashed border-white/10 py-20 text-center"><p class="text-lg font-medium">Your archive is ready</p><p class="mt-2 text-sm text-zinc-500">Add a channel, playlist, or video to begin.</p><a href="{{ route('sources.index') }}" class="primary mt-5 inline-flex">Add a source</a></div>@endif</x-layouts.app>
